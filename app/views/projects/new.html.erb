<P>Add Project</P>
<% flash.each do |type, msg| %>
  <div>
    <div class="alert alert-primary" role="alert">
      <%=msg %>
    </div>
  </div>
<% end %>


<%= form_for :project, url: project_index_path  do |f| %>
  Add task: <%=f.text_field :name %><br>
  <%= f.submit "Add" %>

<% end %>

