<% if @users.any? %>
<% @users.each do |user| %>

  <h3><%=user.name %></h3>

  <%=button_to 'Assign' , projects_developer_index_path({project_id: @project.id, developer_id: user.id}),
             method: :post %><br>
    <%end %>
  <%else %>
  <p>All developers all linked .</p>

<% end %>